---
interface Props {
  data: unknown | unknown[];
}
const { data } = Astro.props;
const items = (Array.isArray(data) ? data : [data]).filter(Boolean);
---
{items.map((d) => <script type="application/ld+json" set:html={JSON.stringify(d)} />)}
